#include "main.h"

/**
 * add - Function to Add to numbers
 * @a: first number to be added to
 * @b: number to add
 * A function that adds two numbers
 * Return: Sum
 */
int add(int a, int b)
{
	return (a + b);
}
